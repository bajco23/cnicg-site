## Packages
(none needed)

## Notes
Uses next-themes (already installed) for light/dark theme toggle.
SEO handled via document.title + meta tag updates in a small client-side utility (no react-helmet dependency).
Contact form submits to POST /api/contact using @shared/routes schemas for validation.
